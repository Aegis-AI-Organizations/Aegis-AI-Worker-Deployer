# Aegis AI Deployer Worker

A Python-based unprivileged worker responsible for provisioning sandbox targets inside the `aegis-war-room`. Instructed rigidly by Temporal to orchestrate vulnerable deployments without violating cluster network policies.
